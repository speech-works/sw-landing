"use client";

export default function RoadmapAppMockup({
    animKey,
    isSectionHovered = false,
    externalMousePos = { x: 0, y: 0 },
}: {
    animKey: number;
    isSectionHovered?: boolean;
    externalMousePos?: { x: number; y: number };
}) {
    void animKey;
    void isSectionHovered;
    void externalMousePos;
    return null;
}
